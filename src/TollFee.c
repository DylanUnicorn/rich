#include"Player.h"
#include"Structure.h"
/*
void GetTollFee(Player *player,Structure *structure,PlayerManager *playermanager){
    if(player->character != structure->owner->character ){
        player->money -= (structure->level+1)*(structure->money)*0.5;
        structure->owner->money += (structure->level)*(structure->money)*0.5;
        IBankruptcy(player,playermanager);        
    }//路人过路费为（房产等级+1）乘以基础金额乘以0.5
}


void IBankruptcy(Player *player,PlayerManager *playermanager){
    if(player->money<0){
        //命令行显示破产函数于此处
        playerManager_removeBankruptPlayer(playermanager,player->character);
    }
}
*/